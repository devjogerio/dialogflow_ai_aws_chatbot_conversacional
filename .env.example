# ==========================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE (.env)
# ==========================================
# Renomeie este arquivo para .env e preencha com seus valores reais.
# NUNCA comite o arquivo .env real no controle de versão.

# ------------------------------------------
# Configurações do Backend (Django)
# ------------------------------------------
# Chave secreta para criptografia (use uma string longa e aleatória em prod)
SECRET_KEY=django-insecure-change-me-in-production-v1
# Modo de debug (True para dev, False para prod)
DEBUG=True

# ------------------------------------------
# Configurações do Banco de Dados (PostgreSQL)
# ------------------------------------------
# Nome do banco de dados
DB_NAME=nexus_db
# Usuário do banco de dados
DB_USER=nexus_user
# Senha do banco de dados
DB_PASSWORD=nexus_password
# Host do banco (nome do serviço no docker-compose)
DB_HOST=db
# Porta do banco
DB_PORT=5432

# ------------------------------------------
# Configurações AWS (Bedrock & OpenSearch)
# ------------------------------------------
# Credenciais IAM com permissão para Bedrock e OpenSearch
AWS_ACCESS_KEY_ID=sua_aws_access_key_id
AWS_SECRET_ACCESS_KEY=sua_aws_secret_access_key
# Região AWS (ex: us-east-1)
AWS_REGION=us-east-1
# Endpoint do OpenSearch Serverless (sem https://)
OPENSEARCH_HOST=seu-id.us-east-1.aoss.amazonaws.com

# ------------------------------------------
# Configurações do Frontend (Next.js)
# ------------------------------------------
# URL da API acessível pelo navegador
NEXT_PUBLIC_API_URL=http://localhost:8000
